// Chart primitives now live in ../shared/Charts (shared by admin analytics and
// the student progress dashboard). Re-exported here so existing admin imports
// keep working unchanged.
export * from "../shared/Charts";
